interface:
  display_name: "Tender Prompt"
  short_description: "Edit, preview, publish, inspect, and use playbooks for Tender Apps"
  default_prompt: "Use $tender-prompt to inspect relevant remote playbooks, create or update this Tender App with the CLI scaffold path, validate it, push through artifact Git, watch the preview, and use the Tender CLI for analytics authoring and dashboards when needed."
policy:
  allow_implicit_invocation: true
