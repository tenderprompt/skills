interface:
  display_name: "Tender Prompt"
  short_description: "Edit, preview, publish, inspect, and use playbooks for Tender Apps"
  default_prompt: "Use $tender-prompt to inspect relevant remote playbooks, preserve an existing app contract or create a new stateless Tender App with the CLI scaffold path, validate it, push through artifact Git, and watch the preview."
policy:
  allow_implicit_invocation: true
